import 'package:flutter/material.dart';

/// Design tokens for Noor for Iman
/// Calm, spiritual, Islamic-inspired palette
class AppColors {
  AppColors._();

  // Primary palette
  static const Color primary = Color(0xFF0E5A4F);       // Deep green
  static const Color primaryLight = Color(0xFF1A7A6A);
  static const Color primaryDark = Color(0xFF083D35);

  // Background
  static const Color background = Color(0xFFF4F1EA);     // Warm beige
  static const Color backgroundDark = Color(0xFF1A1A2E); // Dark mode bg

  // Accent
  static const Color accent = Color(0xFFC7A84B);         // Gold
  static const Color accentLight = Color(0xFFD4BC6A);

  // Surface
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceDark = Color(0xFF252540);

  // Text
  static const Color textPrimary = Color(0xFF2D2D2D);
  static const Color textSecondary = Color(0xFF6B6B6B);
  static const Color textSubtle = Color(0xFF9E9E9E);
  static const Color textOnPrimary = Color(0xFFFFFFFF);
  static const Color textDark = Color(0xFFE8E8E8);

  // Semantic
  static const Color dedication = Color(0xFF9E9E9E);     // Subtle footer text
  static const Color divider = Color(0xFFE0DDD6);
  static const Color shadow = Color(0x1A000000);
}
